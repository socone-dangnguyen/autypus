import 'package:flutter/material.dart';

class MenuExtension extends StatelessWidget {
  const MenuExtension({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
